<h1>Listing Answers</h1>

<table>
  <thead>
    <tr>
      <th>Evaluation</th>
      <th>Question</th>
      <th>Student</th>
      <th>Letra</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @answers.each do |answer| %>
      <tr>
        <td><%= answer.instrument.try(:nombre) %></td>
        <td><%= answer.question.try(:texto) %></td>
        <td><%= answer.student %></td>
        <td><%= answer.letra %></td>
<!--        <td><%= link_to 'Show', instrument_answer_path(@instrument, answer) %></td>-->
<!--        <td><%= link_to 'Edit', edit_instrument_answer_path(@instrument, answer) %></td>-->
<!--        <td><%= link_to 'Destroy', instrument_answer_path(@instrument, answer), method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Answer', new_instrument_answer_path %>
