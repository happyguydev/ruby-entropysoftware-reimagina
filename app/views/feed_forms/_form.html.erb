<%= simple_form_for(@feed_form) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :feedback %>
    <%= f.association :feed_item %>
    <%= f.association :feed_score %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
