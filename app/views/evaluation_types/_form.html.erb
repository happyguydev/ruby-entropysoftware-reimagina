<%= form_for(@evaluation_type) do |f| %>
  <% if @evaluation_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@evaluation_type.errors.count, "error") %> prohibited this evaluation_type from being saved:</h2>

      <ul>
      <% @evaluation_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit "Enviar"%>
  </div>
<% end %>
