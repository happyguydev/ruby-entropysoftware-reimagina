<%= simple_form_for(@proccess_instrument) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :proccess %>
    <%= f.association :instrument %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
